name = "quorum-rooms"
main = "src/index.ts"
compatibility_date = "2026-01-15"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# SQLite-backed Durable Objects are the ones available on the free plan.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]

[vars]
ALLOWED_ORIGINS = "https://quorum-planning-poker.vercel.app"
